import 'package:flutter/material.dart';

class FormDialog extends StatelessWidget {
  final Widget form;
  const FormDialog({Key? key, required this.form}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
